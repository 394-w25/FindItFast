import React from 'react';
import Login from '../components/auth/login';

// Referencing the component to login and signup here.
// Will update this with the authentication logic later... or not.
const SignInPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default SignInPage;